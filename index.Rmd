---
title: "About"
knit: (function(input_file, encoding) {
  out_dir <- 'docs';
  rmarkdown::render(input_file,
 encoding=encoding,
 output_file=file.path(dirname(input_file), out_dir, 'index.html'))})
---
Analysis performed using R (maybe a logo and/or link to R download?) statistical software.

list of R libraries here?

Analysis of emergency medical services (EMS) call volumes for the Denver Health Paramedic Division.

Predictive modeling using weather conditions and home games for professional sports teams

time series analysis.

*make sure to link to each page in the about description

